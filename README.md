# VisionOneWeb
Vision One Website Demo

---

# React + Vite

This project uses the React + Vite setup which provides a minimal configuration with HMR and ESLint rules.

Plugins used:

- @vitejs/plugin-react (Babel for Fast Refresh)
- @vitejs/plugin-react-swc (SWC for Fast Refresh)
